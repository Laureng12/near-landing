"use client"
import React from "react"
import Image from "next/image"
const APP_STORE_URL = "https://apps.apple.com/app/id6744145553"
export default function Page() {
  return (
    <main className="page">
      <TopNav />
      <Hero />
      <PhoneShowcase />
      <MagicStrip />
      <WhyNear />
      <HowItWorks />
      <CoreBenefits />
      <LifeMoments />
      <HouseholdSection />
      <ComparisonStrip />
      <PrivacySection />
      <SocialProof />
      <FAQSection />
      <FinalCTA />
      <SiteFooter />
      <SiteStyles />
    </main>
  
  
  
  
    <spec className="faqSection" id="faq"> sc">
              Smart enough to shut up until it matters. Near stays quiet in the background,
              then shows up exactly when it can help. A rare quality in technology.
            </p>
          </div>
        </div>
      </section>
  )
}
/* -------------------------------- HOW IT WORKS ------------------------------- 
/
function HowItWorks() {
  const steps = [
    {
      num: "01",
      title: "Pin it to a place",
      desc: "Groceries to the store. Returns to the mall. Dry cleaning to the cleaner. Radical concept, honestly.",
      icon: "\uD83D\uDCRD%ompares</span>
        <h2 className="sectionTitle">Near vs. the stuff you&apos;re already trying.</h2> 0 464 0 404V108C0 48 48 0 108 0Z" />
        </clipPath>
      </defs>
      <g clipPath="url(#nearSquircle)">
        <rect width="512" height="512" fill="var(--near-edge)" />
        <circle className="nearPulse" cx="256" cy="256" r="180" fill="url(#nearField)" />
        <rect width="512" height="512" fill="url(#nearVignette)" />
        <circle cx="210" cy="185" r="140" fill="rgba(255,255,255,0.05)" />
      </g>
    </svg>
  )
}
 /* -------------------------------- STYLES ------------------------------ -/
function SiteStyles() {
  return (
    <style jsx global>{`
      :root{
        --near-core:#D8F1FF;
        --near-mid1:#78D7FF;
        --near-mid2:#5C7CFF;
        --near-mid3:#8A63FF;
        --near-edge:#060913;
        --bg0:#04050C;
        --text:#FFFFFF;
        --cyan:#58D9FF;
        --indigo:#6B5CFF;
        --pink:#EC4899;
      }
      html, body {
        padding:0;
        margin:0;
        background: var(--bg0);
        color: var(--text);
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
        scroll-behavior: smooth;
      }
      * { box-sizing:border-box; }
      a { color: inherit; text-decoration:none; }
      img { max-width:100%; display:block; }
      .page{
        min-height:100vh;
        background:
          radial-gradient(1000px 500px at 50% -10%, rgba(88,217,255,0.08), transparent 60%),
          linear-gradient(180deg, #04050c 0%, #050713 32%, #04050c 100%);
      };
        font-weight: 700;
        letter-spacing: -0.03em;
        font-size: 18px;
        opacity: 0.96;
      }
      .hideOnMobile{ display: none }
      @media (prefers-reduced-motion: reduce){
        *, *:before, *:after{
          animation: none !important;
          transition: none !important;
          scroll-behavior: auto !important;
        }
      }
    `</style>
  
  
  
  Ý
